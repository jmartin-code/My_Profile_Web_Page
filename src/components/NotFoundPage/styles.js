import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  NotFoundInfo: {
    textAlign: "center",
    padding: "50px 50px",
    margin: "5%",
    color: "red",
    background: "#D1D0CE",
    borderRadius: "10px",
    boxShadow: "0 2px 5px",
  },
}));
