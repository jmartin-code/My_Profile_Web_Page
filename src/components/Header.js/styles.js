import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  header: {
    flexGrow: 1,
  },
  tabs: {
    backgroundColor: "#fafafa",
    color: "black",
  },
  tabLabel: {
    fontWeight: "bold",
  },
});
