import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  mainGrid: {
    flexGrow: 1,
    minHeight: "80vh",
    paddingTop: "50px",
    paddingBottom: "100px",
  },
  card: {
    maxWidth: 550,
    margin: "auto",
  },
}));
